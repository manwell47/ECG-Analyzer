/**
 * Synthetic dataset (Phase 5 / ADR-006) — public surface.
 *
 * A file-free, deterministic `DatasetAdapter` proving the dataset boundary and
 * providing canonical `SignalRecord`s for hermetic downstream work.
 */
export * from './adapter';
